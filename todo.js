// function addTask(){
// var taskList[0] = document.getElementByName("taskAssignment");
// console.log(taskList[0].id);
// }

// trying to add new tasks in var taskList to the div taskBox
window.onload = function(){
  // var paras = document.querySelectorAll('p');
  // for (var i=0; i<paras.length; i++){
    // console.log(paras[i]);
  var div = "taskBox".createUlNode("ul")

  for(var i =0; i<neighborhood.houses.length; i++){
      console.log(neighborhood.houses[i].roof)
      // create li node
      var li = document.createElement("li")
      // add roof string to li
      li.innerHTML = neighborhood.houses[i].ro

var worker = {
      "name":"Yakko",
      "name":"Wakko",
      "name":"Dot",
    };
